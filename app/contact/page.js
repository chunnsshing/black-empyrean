import ContactClient from '@/components/ContactClient';

export const metadata = {
  title: 'Contact — The Empyrean',
  description:
    "Whether you have a business challenge, a project idea, or just want to explore what's possible — we'd love to hear from you.",
};

export default function ContactPage() {
  return <ContactClient />;
}
