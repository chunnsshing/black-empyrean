import PortfolioClient from '@/components/PortfolioClient';

export const metadata = {
  title: 'Our Work — The Empyrean',
  description:
    'AI products, custom ERP systems, and full-stack builds — delivered across Southeast Asia. Every project started with a real problem.',
};

export default function PortfolioPage() {
  return <PortfolioClient />;
}
