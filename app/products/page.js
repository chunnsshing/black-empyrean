import ProductsClient from '@/components/ProductsClient';

export const metadata = {
  title: 'Products — The Empyrean',
  description:
    'Deployable, customisable software products built on real client engagements — AI agents, operations tooling, analytics, and more. Coming soon.',
};

export default function ProductsPage() {
  return <ProductsClient />;
}
